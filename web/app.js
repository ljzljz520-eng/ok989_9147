const form=document.querySelector('form');const result=document.querySelector('#result');form.addEventListener('submit',async event=>{event.preventDefault();const route=document.querySelector('#route').value;const token=document.querySelector('#token').value;const response=await fetch('/'+route,{headers:{'x-internal-token':token}});result.textContent=JSON.stringify(await response.json(),null,2);});
